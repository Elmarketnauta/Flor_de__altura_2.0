# Reenvío opcional de leads B2B a un CRM o automatización.
# Si se define, /api/b2b-lead hace POST del lead (JSON) a esta URL.
# Ejemplos: webhook de Make, Zapier, HubSpot, Google Apps Script, etc.
# Déjalo vacío para solo registrar el lead en los logs del servidor.
B2B_WEBHOOK_URL=
